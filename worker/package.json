{
  "name": "cultura-basica-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run -c test/vitest.config.ts",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.7",
    "@cloudflare/workers-types": "^5.20260101.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.113.0"
  }
}
