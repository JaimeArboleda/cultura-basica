{
  "name": "cultura-basica",
  "private": true,
  "type": "module",
  "scripts": {
    "build:items": "node data/build-items.mjs",
    "validate:items": "npm run build:items && node data/validate-items.mjs",
    "test:worker": "cd worker && npm test",
    "test:pdf-remesa": "node ocr_tests/verificar_pdf_remesa.mjs",
    "test": "npm run validate:items && npm run test:worker && npm run test:pdf-remesa"
  },
  "devDependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "@pdf-lib/upng": "^1.0.1",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^4.10.38",
    "playwright": "^1.62.1",
    "qrcode-generator": "^2.0.4"
  }
}
