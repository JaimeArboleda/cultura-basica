{
  "name": "cultura-basica",
  "private": true,
  "type": "module",
  "scripts": {
    "build:items": "node data/build-items.mjs",
    "validate:items": "npm run build:items && node data/validate-items.mjs",
    "test:worker": "cd worker && npm test",
    "test": "npm run validate:items && npm run test:worker"
  },
  "devDependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "@pdf-lib/upng": "^1.0.1",
    "pdf-lib": "^1.17.1",
    "playwright": "^1.62.1",
    "qrcode-generator": "^2.0.4"
  }
}
